node_modules/
dist/

# Everything Yarn writes is derived — except the release, which is the point of vendoring it.
.yarn/*
!.yarn/releases
.pnp.*

# Playwright
playwright-report/
test-results/

# Coverage, for every workspace that measures it. Here rather than per package: umbra and antumbra
# each ignored their own, so the first package to add a `test:coverage` — limb — would have
# committed the HTML report instead.
coverage/
.nyc_output/

*.log
.DS_Store
Thumbs.db

# Assembled by the deploy: each playground is its own build, dropped in before the site is built.
apps/home/public/playground/

# The deploy artifact.
francisdesjardins.ca-dist.zip

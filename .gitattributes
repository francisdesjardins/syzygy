# LF everywhere, whatever the checkout's platform. `yarn.lock` had been committed CRLF from Windows,
# and a line ending nobody can see is still a byte every tool that reads the file has to agree on.
* text=auto eol=lf

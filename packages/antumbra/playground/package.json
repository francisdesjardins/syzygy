{
  "name": "antumbra-playground",
  "version": "0.0.0",
  "private": true,
  "description": "Interactive demo app and reference template implementations for antumbra. Not published.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:file": "VITE_HASH_ROUTER=true vite build",
    "preview": "vite preview",
    "check": "echo \"antumbra-playground: no-op by design — the parent runs tsc -p playground/tsconfig.json --noEmit and lints playground/** through its own .oxlintrc.json\"",
    "test": "echo \"antumbra-playground: no-op by design — its 24 test files run in the parent playwright projects, testMatch {src,playground/src}/**\""
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/material": "^9.4.0",
    "@tanstack/react-router": "^1.170.36",
    "corona": "workspace:^",
    "immer": "^11.1.18",
    "limb": "workspace:^",
    "penumbra": "workspace:^",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "react-syntax-highlighter": "^16.1.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "rolldown": "^1.2.8",
    "vite": "^8.3.0"
  },
  "engines": {
    "node": ">=24"
  }
}
